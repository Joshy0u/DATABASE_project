name: Backend Docker CI 

on:
  push:
    paths:
      - "backend/**"
  pull_request:
    paths:
      - "backend/**"

# changing from code verification to docker build verification
# now changing from no tagging, to github tagging for tracability
jobs: 
  build-docker:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set image tag via GIT SHA
        run: echo "IMAGE_TAG=${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: LOGIN DOCKERHUB
        run: |
          echo "${{ secrets.DOCKER_HUB_TOKEN }}" | docker login -u "${{ secrets.NAME }}" --password-stdin
      
      - name: Build backend Docker image (Backend+SHA)
        run: |
          docker build \
          -t ${{ secrets.NAME }}/databaseproject:backend-${IMAGE_TAG} \
          ./backend

      - name: Verify image exists
        run: |
          docker images | grep backend
      
      - name: Push to DOCKER HUB 
        run: |
          docker push ${{ secrets.NAME }}/databaseproject:backend-${IMAGE_TAG}

      - name: Checkout Infrastructure repo 
        uses: actions/checkout@v4 
        with:
          repository: Joshy0u/k8s-infrastructure
          token: ${{ secrets.INFRA_REPO_PAT }}
          path: infra-repo 

      - name: Update Image Tag in Infrastructure repo
        run: |
          cd infra-repo 
          #find, and update step below:
          sed -i "s|image: ${{ secrets.NAME }}/databaseproject:backend-.*|image: ${{ secrets.NAME }}/databaseproject:backend-${IMAGE_TAG}|g" manifests/backend/deployment.yaml

          git config --global user.name "GitOps Bot" 
          git config --global user.email "gitops-bot@users.noreply.github.com"

          #commit and push
          git add manifests/backend/deployment.yaml
          git commit -m "chore: update backend image to backend-${IMAGE_TAG}"
          git push origin main 
